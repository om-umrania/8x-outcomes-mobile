/**
 * DEMO_MODE: the app must remain demonstrable without network, LLM availability,
 * human respondents, or API availability (8x-outcomes-brief pivot, "DEMO MODE").
 * Live APIs are an enhancement, not a dependency for judging — see DECISIONS.md D-014.
 * Fixed true for this build; a future LiveRunner flips this per ROADMAP.md "Then".
 */
export const DEMO_MODE = true;
